


import 'package:flutter/material.dart';

class AppColors {
  // 🟦 Primary color — brand color (used for buttons, accents)
  static const Color primary = Color(0xFF1E88E5); // Blue 600

  // 🟩 Secondary color — highlights, secondary actions
  static const Color secondary = Color(0xFF43A047); // Green 600

  // ⚫ Neutral shades — backgrounds, text, borders
  static const Color background = Color(0xFFF5F5F5); // Light gray background
  static const Color text = Color(0xFF212121); // Almost black for good contrast

  // 🔴 Optional: error color (for alerts or invalid inputs)
  static const Color error = Color(0xFFE53935); // Red 600
}
